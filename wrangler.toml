name = "visitkmg"
compatibility_date = "2026-06-26"
main = "worker.js"

# Static site (HTML/CSS/JS) served straight from the repo root.
# .assetsignore keeps node_modules, backend source and secrets out of the upload.
[assets]
directory = "."
binding = "ASSETS"
not_found_handling = "404-page"
